# Dashboard Favorites Quick Task Summary

Filtered the dashboard recent songs logic to only show favorites (`isFavorite === true`). Changed the translation text to "Favorites".

`git commit -m "feat(dashboard): show only favorite songs on dashboard screen"`
